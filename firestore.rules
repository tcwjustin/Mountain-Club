rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 登入後的隊員可讀寫社團共用資料
    match /clubs/{clubId} {
      allow read, write: if request.auth != null;
    }
  }
}
